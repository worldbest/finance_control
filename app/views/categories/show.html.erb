<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @category.name %> <br>
  <strong>Children:</strong> <br>
  <%= render 'subcategory', categories: @category.root.children %>
</p>

<%= link_to 'Edit', edit_category_path(@category) %> |
<%= link_to 'Back', categories_path %>
